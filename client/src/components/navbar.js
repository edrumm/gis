import { useState } from 'react';
import './style/navbar.css';

const Navbar = () => {
    return(
        <>
        <nav>
            Navigation Bar
        </nav>
        </>
    );
};

export default Navbar;